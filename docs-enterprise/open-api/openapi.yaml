# Copyright 2024 Datastrato Inc.

---
openapi: 3.0.3
info:
  title: Datastrato REST API
  version: 1.0.0-SNAPSHOT-1.0.0-SNAPSHOT
  description: |
    Defines the specification for the Datastrato Gravitino REST API.


servers:
  - url: "{scheme}://{host}:{port}/{basePath}"
    description: Generic base server URL, with all parts configurable.
    variables:
      scheme:
        description: The scheme of the URI, either http or https.
        default: http
      host:
        description: The host address for the specified server
        default: localhost
      port:
        description: The port used when addressing the host
        default: "8090"
      basePath:
        description: Optional prefix to be appended to all routes
        default: "api"

security:
  - OAuth2WithJWT: [serviceAudience]
  - BasicAuth: []

paths:
  /search/query:
    $ref: "./search.yaml#/paths/~1search~1query"

  /search/rebuild/metalakes/{metalake}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./search.yaml#/paths/~1search~1rebuild~1metalakes~1%7Bmetalake%7D"

  /search/sync/metalakes/{metalake}/objects:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./search.yaml#/paths/~1search~1sync~1metalakes~1%7Bmetalake%7D~1objects"

  /search/task/{taskId}/status:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./search.yaml#/paths/~1search~1task~1%7BtaskId%7D~1status"

  /web/metalakes/{metalake}/connections/{connection}/credential-providers/{credentialType}/test:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./credential-providers.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1connections~1%7Bconnection%7D~1credential-providers~1%7BcredentialType%7D~1test"

  /web/metalakes/{metalake}/connections:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./connections.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1connections"

  /web/metalakes/{metalake}/connections/{connection}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./connections.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1connections~1%7Bconnection%7D"

  /web/metalakes/{metalake}/policies:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./policies.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1policies"

  /web/metalakes/{metalake}/summary:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./metalake-summary.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1summary"

  /web/metalakes/{metalake}/tags:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./tags.yaml#/paths/~1web~1metalakes~1%7Bmetalake%7D~1tags"

  /web/entities:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./entities.yaml#/paths/~1web~1entities"

  /web/with-tags/metalakes/{metalake}/catalogs:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs"

  /web/with-tags/metalakes/{metalake}/catalogs/{catalog}/schemas:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas"

  /web/with-tags/metalakes/{metalake}/catalogs/{catalog}/schemas/{schema}/tables:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas~1%7Bschema%7D~1tables"

  /web/with-tags/metalakes/{metalake}/catalogs/{catalog}/schemas/{schema}/filesets:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas~1%7Bschema%7D~1filesets"

  /web/with-tags/metalakes/{metalake}/catalogs/{catalog}/schemas/{schema}/topics:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas~1%7Bschema%7D~1topics"

  /web/with-tags/metalakes/{metalake}/catalogs/{catalog}/schemas/{schema}/models:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./creation-with-tags.yaml#/paths/~1web~1with-tags~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas~1%7Bschema%7D~1models"

  /web/preview/metalakes/{metalake}/catalogs/{catalog}/schemas/{schema}/tables/{table}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./preview.yaml#/paths/~1web~1preview~1metalakes~1%7Bmetalake%7D~1catalogs~1%7Bcatalog%7D~1schemas~1%7Bschema%7D~1tables~1%7Btable%7D"

  /web/metrics/metalakes/{metalake}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./metrics.yaml#/paths/~1web~1metrics~1metalakes~1%7Bmetalake%7D"

  /web/security/metalakes/{metalake}/roles/{role}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./permission.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1roles~1%7Brole%7D"

  /web/security/metalakes/{metalake}/roles/assignments:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./permission.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1roles~1assignments"

  /web/security/metalakes/{metalake}/roles/{role}/users:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./permission.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1roles~1%7Brole%7D~1users"

  /web/security/metalakes/{metalake}/roles/{role}/groups:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./permission.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1roles~1%7Brole%7D~1groups"

  /web/security/metalakes/{metalake}/roles:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./authorization.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1roles"

  /web/security/metalakes/{metalake}/objects/roles:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./authorization.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1objects~1roles"

  /web/security/metalakes/{metalake}/authorization/overview:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./authorization.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1authorization~1overview"

  /web/security/metalakes/{metalake}/users/{user}/roles:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./authorization.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users~1%7Buser%7D~1roles"

  /web/security/metalakes/{metalake}/groups/{group}/roles:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./authorization.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1groups~1%7Bgroup%7D~1roles"

  /web/security/metalakes/{metalake}/users:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./users.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users"

  /web/security/identity/users/{username}/groups:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./identity-lookups.yaml#/paths/~1web~1security~1identity~1users~1%7Busername%7D~1groups"

  /web/security/identity/groups/{groupName}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./identity-lookups.yaml#/paths/~1web~1security~1identity~1groups~1%7BgroupName%7D"

  /web/security/directory/users:
    $ref: "./users.yaml#/paths/~1web~1security~1directory~1users"





  /web/security/metalakes/{metalake}/users/enabled:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./users.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users~1enabled"

  /web/security/metalakes/{metalake}/users/{user}:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./users.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users~1%7Buser%7D"

  /web/security/metalakes/{metalake}/users/{user}/groups:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./users.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users~1%7Buser%7D~1groups"

  /web/security/metalakes/{metalake}/users/idp:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./users.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1users~1idp"

  /web/security/metalakes/{metalake}/groups/idp:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./groups.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1groups~1idp"

  /web/security/metalakes/{metalake}/groups:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./groups.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1groups"

  /web/security/metalakes/{metalake}/groups/{group}/users:
    # escape the path name by replacing `{ }` with `%7B %7D`
    $ref: "./groups.yaml#/paths/~1web~1security~1metalakes~1%7Bmetalake%7D~1groups~1%7Bgroup%7D~1users"

components:
  securitySchemes:

    OAuth2WithJWT:
      type: oauth2
      description: OAuth2 with JWT
      flows:
        clientCredentials:
          tokenUrl: https://external-oauth-server.com/token
          scopes:
            serviceAudience: The audience name when Gravitino uses OAuth as the authenticator.

    BasicAuth:
      type: http
      scheme: basic

    KerberosAuth:
      type: http
      scheme: negotiate
