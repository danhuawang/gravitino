# Copyright 2026 Datastrato Inc.

---

paths:

  /web/security/metalakes/{metalake}/users:
    parameters:
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/metalake"
    get:
      tags:
        - users
      summary: List metalake users for the security UI
      operationId: listUsersWithGroups
      description: |
        List users under a metalake for the enterprise security Users page.

        Returns OSS user fields plus `origin` and metalake `groups`. `origin` is `Local` when the
        username exists in the built-in IdP (`idp_user_meta`) and `Provisioned` otherwise.
        `enabled` maps to Active / Suspended in the UI. `groups` lists metalake group names for
        the Users table.

      responses:
        "200":
          description: Returns the list of users
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "#/components/schemas/ExtendedUserListResponse"
        "404":
          description: Not Found - The target metalake does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

    post:
      tags:
        - users
      summary: Add an existing local user into a metalake
      operationId: addSecurityUser
      description: |
        Add an existing built-in IdP user into a metalake for the enterprise security Users page.

        Requires the username to already exist in the built-in IdP. If it does not, returns 404 and
        does not write `user_meta`. If the username is already in `user_meta` for the metalake,
        returns 409. Otherwise creates the metalake `user_meta` row (`enabled` defaults to true /
        Active) and optionally grants metalake roles. This API does not create login credentials.

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LocalUserAddRequest"
            examples:
              LocalUserAddRequest:
                $ref: "#/components/examples/LocalUserAddRequest"

      responses:
        "200":
          description: Returns the metalake user
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "#/components/schemas/ExtendedUserResponse"
        "400":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/BadRequestErrorResponse"
        "404":
          description: Not Found - The target metalake or IdP user does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
        "409":
          description: Conflict - The metalake user already exists
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

  /web/security/metalakes/{metalake}/users/enabled:
    parameters:
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/metalake"
    put:
      tags:
        - users
      summary: Batch update user enabled
      operationId: batchUpdateUserEnabled
      description: |
        Batch-update the `enabled` flag for metalake users by username.

        The update validates first that every listed user exists under the metalake and has no
        `externalId`. Only then is a single UPDATE executed. Otherwise the request fails and no user
        is updated.

        Missing metalake returns 404. Missing users or users with `externalId` return 400.

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserEnabledBatchUpdateRequest"

      responses:
        "200":
          description: Returns the updated user names
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/NameListResponse"
        "400":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/BadRequestErrorResponse"
        "404":
          description: Not Found - The target metalake does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

  /web/security/metalakes/{metalake}/users/{user}:
    parameters:
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/metalake"
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/user"
    get:
      tags:
        - users
      summary: Get a metalake user for the security UI
      operationId: getSecurityUser
      description: |
        Get a metalake user for the enterprise security user Overview page.

        Returns OSS user fields plus `origin`. `origin` is `Local` when `externalId` is blank and
        `Provisioned` when `externalId` is present.

      responses:
        "200":
          description: Returns the user
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "#/components/schemas/ExtendedUserResponse"
        "404":
          description: Not Found - The target metalake or user does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
                NoSuchUserException:
                  $ref: "../../docs/open-api/users.yaml#/components/examples/NoSuchUserException"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

  /web/security/metalakes/{metalake}/users/{user}/groups:
    parameters:
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/metalake"
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/user"
    get:
      tags:
        - users
      summary: List groups for a metalake user
      operationId: listSecurityUserGroups
      description: |
        List metalake groups the user belongs to for the security user Groups tab.

        Local users (`externalId` blank) resolve membership from the built-in IdP. Provisioned users
        resolve membership from SCIM. Each group includes `origin` and the group's metalake roles.

      responses:
        "200":
          description: Returns the groups
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "./groups.yaml#/components/schemas/ExtendedGroupListResponse"
        "404":
          description: Not Found - The target metalake or user does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
                NoSuchUserException:
                  $ref: "../../docs/open-api/users.yaml#/components/examples/NoSuchUserException"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

  /web/security/metalakes/{metalake}/users/idp:
    parameters:
      - $ref: "../../docs/open-api/openapi.yaml#/components/parameters/metalake"
    get:
      tags:
        - users
      summary: List built-in IdP users with metalake membership status
      operationId: listSecurityIdpUsers
      description: |
        List built-in IdP users for the security UI picker used when adding a local user.

        Each item has `name` and `status`. `status` is `true` when that username is already in
        this metalake.

      responses:
        "200":
          description: Returns the list of IdP users
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "#/components/schemas/IdpUserNameListResponse"
        "404":
          description: Not Found - The target metalake does not exist
          content:
            application/vnd.gravitino.v1+json:
              schema:
                $ref: "../../docs/open-api/openapi.yaml#/components/schemas/ErrorModel"
              examples:
                NoSuchMetalakeException:
                  $ref: "../../docs/open-api/metalakes.yaml#/components/examples/NoSuchMetalakeException"
        "5xx":
          $ref: "../../docs/open-api/openapi.yaml#/components/responses/ServerErrorResponse"

components:
  schemas:
    IdentitySource:
      type: string
      description: |
        Source of the identity. Serialized as `origin`. `Local` when the name exists in the
        built-in IdP (`idp_user_meta`) and `Provisioned` otherwise.
      enum:
        - Local
        - Provisioned

    IdentityType:
      type: string
      description: Identity type for user or group lookup before add
      enum:
        - local
        - provisioned

    UserGroupNamesResponse:
      type: object
      properties:
        code:
          type: integer
          format: int32
          description: Status code of the response
          enum:
            - 0
        username:
          type: string
          description: Looked-up username
        type:
          $ref: "#/components/schemas/IdentityType"
        groupNames:
          type: array
          description: Group names for the user
          items:
            type: string

    IdpNameStatus:
      type: object
      required:
        - name
        - status
      properties:
        name:
          type: string
          description: Built-in IdP username or group name
        status:
          type: boolean
          description: >
            Whether the identity is already added to this metalake. `true` means it already exists
            in the metalake.

    IdpUserNameListResponse:
      type: object
      properties:
        code:
          type: integer
          format: int32
          description: Status code of the response
          enum:
            - 0
        users:
          type: array
          description: Built-in IdP users with metalake membership status
          items:
            $ref: "#/components/schemas/IdpNameStatus"

    ExtendedUser:
      allOf:
        - $ref: "../../docs/open-api/users.yaml#/components/schemas/User"
        - type: object
          required:
            - origin
            - groups
          properties:
            origin:
              $ref: "#/components/schemas/IdentitySource"
            groups:
              type: array
              description: Metalake group names for the security Users table
              items:
                type: string

    ExtendedUserListResponse:
      type: object
      properties:
        code:
          type: integer
          format: int32
          description: Status code of the response
          enum:
            - 0
        users:
          type: array
          description: A list of users with origin for the security UI
          items:
            $ref: "#/components/schemas/ExtendedUser"

    UserEnabledBatchUpdateRequest:
      type: object
      required:
        - users
        - enabled
      properties:
        users:
          type: array
          items:
            type: string
          description: User names to update under the metalake
        enabled:
          type: boolean
          description: Target enabled value for all listed users

    LocalUserAddRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
          pattern: "^[^:]*$"
          description: >
            The username to add to the metalake. Must already exist in the built-in IdP.
        roles:
          type: array
          items:
            type: string
            minLength: 1
          description: Optional metalake roles to grant to the user
        enabled:
          type: boolean
          description: Metalake user enabled flag; defaults to true (Active)

    ExtendedUserResponse:
      type: object
      properties:
        code:
          type: integer
          format: int32
          description: Status code of the response
          enum:
            - 0
        user:
          $ref: "#/components/schemas/ExtendedUser"

  examples:
    LocalUserAddRequest:
      value:
        name: jordan.reyes
        roles:
          - Analyst
        enabled: true
